import { Text, View } from "react-native";

export default function LogIn() {
  return (
    <View
        style = {{
          flex: 1,
          justifyContent: "center",
          alignItems: "center",
        }}
    >
        <Text></Text>
    </View>
    );

}